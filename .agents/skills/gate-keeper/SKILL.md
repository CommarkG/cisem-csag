---
name: "gate-keeper"
description: "Audits repository state against LGG compliance gate validations 0 to 18."
version: "1.0"
---

# Gate Keeper Compliance Skill

This skill enforces strict security, architectural boundaries, and coding rules across the CISEM repository using the gate check verifiers.

## Invocation

Run the gate keeper checks using:
```bash
python cisem_core/platform_core/cisem_gate.py --all
```
