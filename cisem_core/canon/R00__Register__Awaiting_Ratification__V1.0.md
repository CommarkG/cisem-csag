# R00 · Register · Awaiting Governor Ratification

**Purpose:** every judgment call made by the reviewer rather than the governor, in one place.
**Rule:** an item here is **not canon** until ratified. Documents carrying it are drafts.

**How to use:** approve by address (`R00.3.02 approved`), reject with a reason, or ask. Anything not decided stays here.

---

## R00.1 — What is already ratified

Explicitly decided in session and not in question:

`U0A` tiers · `U0B` naming grid · `U1`–`U7` all 66 decisions · the UVP · identity provider choice · `app_metadata` · one-way flow · single active scope · staleness split · broker over trigger · version-locked snapshots · canon home inside the working boundary · zone direction · durability ladder · verification doctrine · inheritance obligation · exit gates · first-cycle criterion.

---

## R00.2 — Structural decisions awaiting ratification

| # | Decision | Made by | If rejected |
|---|---|---|---|
| `R00.2.01` | `U0C` holds only the invariant and pocket **formats**; the cycle principles reside in `U1` | reviewer | `U0C` is rewritten as a full cycle-law document |
| `R00.2.02` | The duplicate cycle-law `U0C` was **deleted**, not superseded — it was never ratified, so nothing was lost | reviewer | It is restored and marked superseded instead |
| `R00.2.03` | Supersession tables are instance facts; they moved out of `U0A`/`U0B` to `I1.2.10` | reviewer | They return to the canon-law tier |
| `R00.2.04` | `U0A.7` now states only the migration *rule*; the project mapping moves to `I1` | reviewer | The mapping returns to `U0A` |

---

## R00.3 — Pipeline decisions awaiting ratification

**`P1` is entirely reviewer-authored.** Nothing in it inherits a prior decision.

| # | Decision | If rejected |
|---|---|---|
| `R00.3.01` | Nine stages, this decomposition | Restructure |
| `R00.3.02` | Verification method **fixed at stage 5**, unchangeable at stage 8 | A weaker check may be substituted when the fixed one fails |
| `R00.3.03` | Enforcement tier decided at stage 6, **before** building | Tier chosen after, when it no longer shapes the build |
| `R00.3.04` | No stage skipped, however small the solution | Small work bypasses the method |
| `R00.3.05` | Consolidation between every solution, never batched | Frictions accumulate and are reconstructed from memory |
| `R00.3.06` | Stage 1 rejects intent that names a mechanism | Solutions arrive pre-decided |
| `R00.3.07` | Stage 4 inventory must be read from source, never a summary | The second-store defect recurs |

---

## R00.4 — Solution register decisions awaiting ratification

| # | Decision | If rejected |
|---|---|---|
| `R00.4.01` | Record format: one section per pipeline stage plus consolidation | Restructure |
| `R00.4.02` | Solutions numbered by **entry order**, not priority | Renumbering as priorities change |
| `R00.4.03` | **Observability is `S01`** — the only unblocked candidate, and required before any external party | Another candidate enters, or none does |
| `R00.4.04` | Entry requires the cumulative invariant suite to pass | Solutions begin while prior ground has stopped holding |

---

## R00.5 — Instance classifications awaiting ratification

Assigned by the reviewer from names and partial evidence.

| # | Decision | Evidence | If wrong |
|---|---|---|---|
| `R00.5.01` | Nine entities classified **core** from their names alone | inferred | Wrong rule shape on up to 9 entities |
| `R00.5.02` | Four entities classified **vertical** from their names alone | inferred | Same |
| `R00.5.03` | CRM entities left **unclassified**, pending `I3.5.10` | open | Blocks their rule pattern |
| `R00.5.04` | Thirty source paths assigned zones in the manifest | partly inferred | Import linter enforces a wrong boundary |
| `R00.5.05` | Ten items placed in the deferral register with conditions | reviewer | Work is blocked, or unblocked, wrongly |
| `R00.5.06` | Sixteen mechanisms assigned canonical addresses across topics | reviewer | Renumbering |

---

## R00.6 — Sequencing awaiting ratification

| # | Decision | Basis |
|---|---|---|
| `R00.6.01` | The identity chain (`I2.5.01`–`I2.5.03`) must precede the credential swap | Deploying first is a total outage |
| `R00.6.02` | The linter reaching zero (`I3.5.05`) gates the credential swap | Swapping first takes the catalog dark |
| `R00.6.03` | All product work (`I5`, `I6`) is blocked until the boundary is enforced | Building on an unenforced boundary produces work to be rebuilt |
| `R00.6.04` | E9 is the highest-value unbuilt mechanism | It moves the most-repeated regression from T1 to T3 |
| `R00.6.05` | Canon into the working boundary is the single highest-leverage remaining item | Until then no pocket can be assembled and the canon governs the human only |

---

## R00.7 — Reviewer errors, recorded

Per `U1.2.11`, corrections are recorded rather than deleted.

| # | Error | Caught by | Corrected at |
|---|---|---|---|
| `R00.7.01` | Claimed the catalog layer had no tenant protection; isolation was enabled, only rules were missing | the governor's enablement listing | `I3.1.06` |
| `R00.7.02` | Called the entitlement tables a built tiering system; they are empty scaffolding | querying them | `I4.1.01` |
| `R00.7.03` | Recommended solution work on an item deferred one turn earlier | the governor | `U0A.1` |
| `R00.7.04` | Stated one law in two documents, twice | the X0 gate | `U1.2.13`, `U1.2.14` |
| `R00.7.05` | Ran two addressing schemes in parallel without declaring one dead | building the naming grid | `U0B.2.09` |
| `R00.7.06` | Created two documents at address `U0C` | the tier check | `R00.2.02` |
| `R00.7.07` | Left project file names and a date inside canon-law documents | the tier check | `R00.2.03` |

**Pattern:** all seven were invisible from inside a single document and surfaced only from a check spanning several. **Consistency defects live between artifacts.**

---

## R00.8 — What is not ready

| Item | State |
|---|---|
| The four document sets | **Ready for review.** Not canon until this register is decided. |
| The canon in the repository | Not moved — no actor can read it |
| The zone manifest | Incomplete — awaits enumeration |
| The identity chain | Not started |
| Twenty-five entities without rules | Open |
| The isolation boundary | **Built and inert** — the application still bypasses it |
| Observability | Absent |
| Environment separation | Absent |

**The documents are ready. The system is not.**

---

## R00.9 — Assurance & lifecycle decisions awaiting ratification

**Made by the reviewer at the governor's direction, to be decided when attention allows.** All are additive — none alters a ratified item.

### R00.9.1 — Assurance doctrine · `U1.2.30`

| # | Decision | If rejected |
|---|---|---|
| `R00.9.01` | Seven assurance acts named and distinguished: prevention · constraint · check · verification · validation · audit · consolidation | The three silent-failure acts stay unnamed and therefore unbuilt |
| `R00.9.02` | Verification and validation are **different acts** — right-built versus right-thing | Useless-but-correct solutions pass undetected |
| `R00.9.03` | An audit is **not** a check — it compares artifacts to each other, not a change to rules | Between-artifact defects remain undetectable; all seven reviewer errors were of this class |
| `R00.9.04` | Constraints are carried **per step**, only those governing that step, never repeated identically | Distance in context equals absence; identical blocks produce blindness |
| `R00.9.05` | A constraint is enforcement only when the step **cannot close without producing its exit evidence** — the output itself, never an assertion | A step pocket is T1 in a better location |

### R00.9.2 — Validation stage · `P1.10`

| # | Decision | If rejected |
|---|---|---|
| `R00.9.06` | The pipeline gains a tenth stage, **Validate**, closing back to stage 1 | The pipeline ends at Record and never asks whether the intent was achieved |
| `R00.9.07` | Renamed **Intent to Validated** | — |
| `R00.9.08` | Validation occurs **after real use**, not at delivery | It measures the builder's confidence rather than the outcome |
| `R00.9.09` | "Did not achieve" is a **successful** validation | Validation becomes a formality that always passes |

### R00.9.3 — Audit cadence · `U7.2.10`

| # | Decision | If rejected |
|---|---|---|
| `R00.9.10` | Five audits defined: cross-artifact consistency · tier integrity · durability drift · reference liveness · coverage mirror | No scheduled comparison across artifacts |
| `R00.9.11` | Audits run at **cycle exit**, not per change | Between-artifact defects accumulate to the next major review |
| `R00.9.12` | **An audit that never fails is not running** | A clean result is mistaken for a healthy system |

### R00.9.4 — Template lifecycle · `U4.2.30`

Grounded in researched practice — theme-update models, API deprecation policy, date-based version pinning. Sources noted in session.

| # | Decision | If rejected |
|---|---|---|
| `R00.9.13` | **The customisation boundary is declared before the first template ships** — configuration survives updates, forks do not | Every upgrade becomes a manual merge for both parties, permanently |
| `R00.9.14` | Three change classes: fix and additive apply automatically; only breaking is opt-in | Every change becomes a negotiation, or none does |
| `R00.9.15` | **Additive by default** — a change is designed as additive unless it cannot be | Version count grows with change count |
| `R00.9.16` | A breaking update arrives **unpublished**, beside the live version | Scopes are upgraded without their act |
| `R00.9.17` | **Exactly one** previous version retained as the rollback point | Unbounded history, or no rollback at all |
| `R00.9.18` | **N−1 support with a calendar floor** — no version retired in under a stated minimum, whatever the release cadence | A fast cycle forces continuous upgrades, defeating opt-in |
| `R00.9.19` | Sunset is phased and driven by **measured** usage | Retirement by schedule, breaking whoever still depends on it |

### R00.9.5 — Plan template

| # | Decision | Status |
|---|---|---|
| `R00.9.20` | `U0D` plan template — every plan begins as a copy | **not drafted** — awaits `R00.9.04`, `R00.9.05` |
| `R00.9.21` | The template carries **addresses, never rule text** — copy the address, never the law | proposed; prevents a hundred drifting copies |
| `R00.9.22` | A step closes by **pasting its exit evidence into the step**, not by ticking a box | proposed; this is the whole difference between a reminder and a gate |

**Adoption risk, stated plainly:** if the first few plans leave their evidence fields empty, every later plan learns the field is optional and the template becomes decoration. That is not a design flaw — it is the real risk, and no mechanism removes it.

---

## R00.10 — What this adds to the open work

| Item | Blocks |
|---|---|
| The customisation boundary (`R00.9.13`) | Must precede the first template shipped to any external party |
| Usage tracking for versions (`R00.9.19`) | Prerequisite for having versions at all — cannot sunset what you cannot measure |
| Observability | Now blocks `R00.9.19` as well as `S01` |

---

## R00.11 — Load doctrine decisions awaiting ratification · `U1.2.31`

Derived from `I1.3.50`, a full audit of the building actor's 28 defects.

| # | Decision | If rejected |
|---|---|---|
| `R00.11.01` | Most defects are **held-state failures**, not knowledge failures — two-thirds of audited incidents | Prevention continues to target knowledge, which was never the gap |
| `R00.11.02` | **Detection adds to load and does not prevent load failures.** Correct only where the defect is undetectable from the artifact, or exists between artifacts. | The mechanism set keeps growing and the failure rate does not fall |
| `R00.11.03` | One decision per step; a step producing several is several steps | Long outputs continue to drop constraints applying to their middle |
| `R00.11.04` | Constraints travel **with the step**, only that step's, restated before producing | Session-level constraints keep failing at step nine |
| `R00.11.05` | Lists are **enumerated from a source** and closed with a count assertion | Composed lists remain plausible and incomplete |
| `R00.11.06` | Facts are read at use or carried with their source; never recalled | Confident wrong values keep surviving review |
| `R00.11.07` | **"I cannot obtain this" must be an available step outcome** | Fabrication remains the only way to complete an impossible step |
| `R00.11.08` | Mandated elements name their derivation | Ritual elements stay stale and unnoticed |

### R00.11.1 — Reviewer's self-assessment, recorded

`R00.11.09` — **The mechanism set I designed is 14/16 detection and 2/16 structural.** The two structural mechanisms are the two that verifiably held under test. This is a weighting error on my part, identified by the governor and confirmed by the audit.

`R00.11.10` — **No mechanism I proposed reduces working-set load.** Every one adds a requirement. Against a root cause that is two-thirds of all incidents, that is the wrong instrument.

`R00.11.11` — **The plan template is the primary R1 instrument and remains undrafted.** It has been deferred twice in favour of mechanism work.

---

## R00.12 — Axiom, rhythm, and loops awaiting ratification

### R00.12.1 — Prevention axiom · `U1.2.32`

| # | Decision | If rejected |
|---|---|---|
| `R00.12.01` | **Prevention beats correction, every time** — adopted as a stance, not a calculation | Each case is re-argued from scratch, and the cheap-now option wins by default |
| `R00.12.02` | Four conditions where the axiom is **absolute**, no cost comparison: irreversible · undetectable · silent by construction · compounding | Prevention gets traded away in exactly the cases where correction does not exist |
| `R00.12.03` | The floor: prevention that adds more load than it removes is a new defect. Over-prevention is **relocated down the ladder**, not abandoned. | Either paralysis, or the axiom is quietly dropped when it becomes inconvenient |
| `R00.12.04` | **Every correction closes with "what would have prevented this?"** — "more care" is not an answer | Corrections repeat and the pattern is never named |

### R00.12.2 — Spiral rhythm · `U1.2.33`

| # | Decision | If rejected |
|---|---|---|
| `R00.12.05` | A cycle has two required half-cycles: **expansion** and **consolidation**. Neither corrects the other. | Expansion is treated as mess-making and gradually stops happening |
| `R00.12.06` | **Consolidation is a stage, not a verdict.** A cycle needing none should be examined for whether it explored anything. | Teams optimise for producing nothing to consolidate |
| `R00.12.07` | Three consolidation outcomes — **convergence · divergence · violation** — and **only violation is a defect** | A law arrived at twice is filed as an error, discarding the strongest promotion signal available |
| `R00.12.08` | **Optimisation belongs to consolidation** | It becomes a separate initiative that is never scheduled |

### R00.12.3 — The six loops · `U1.2.34`

| # | Decision | If rejected |
|---|---|---|
| `R00.12.09` | Six named loops: **capture · place · synthesise · promote · implement · prove** | The acts stay conflated and the missing ones are never noticed |
| `R00.12.10` | **L1 Capture at the moment**, never reconstructed — the only loop with no downstream repair | Observations arrive stripped of the context that made them worth having |
| `R00.12.11` | **L3 is comparison, not accumulation.** Records piling up is not synthesis. | A growing archive that nobody learns from |
| `R00.12.12` | **L4 promotes at two occurrences**, and demotes laws no context has needed | The universal fills with elegant unused rules |
| `R00.12.13` | **L6: a mechanism counts only after failing on a known-bad input** | An always-passing check is indistinguishable from a working one |
| `R00.12.14` | **All six loops run at all four tiers** | A tier stops learning while appearing to function |

### R00.12.4 — Reviewer note

`R00.12.15` — **The consolidation reframe corrects my framing, not the governor's.** I had written consolidation entirely in defect language — frictions, findings, contradictions. That treats expansion as something to apologise for. The governor's framing is better and is now the ratified one.

`R00.12.16` — **L1 Capture is the weakest link and is currently unenforced.** It has no mechanism, no artifact, and no trigger beyond intention. Everything in the other five loops depends on it, and it is the one place where a miss is unrecoverable.

---

## R00.13 — Governance-as-surface decisions awaiting ratification · `U6.2.09`

**Governor-originated.** Recorded here for the record and for the derived items, not because the demand itself is in question.

| # | Decision | If rejected |
|---|---|---|
| `R00.13.01` | **Every governing definition is visible in the administrative interface. No exceptions.** | Definitions stay in documents and conversation; the operator carries the index in their head |
| `R00.13.02` | **Visible is mandatory; editable is not** — this is what makes "no exceptions" achievable | Cost of building editors becomes the reason elements stay hidden |
| `R00.13.03` | Each surfaced definition shows address · statement · status · tier · enforced-by · source · last-verified | A surface that shows the rule but not its enforcement hides the weakness |
| `R00.13.04` | **"Enforced by: nothing" must be displayable** | The system can only show its strengths |
| `R00.13.05` | Sixteen mandatory operator surfaces | Partial coverage, chosen by convenience |
| `R00.13.06` | Five mandatory scope-facing surfaces — **a scope may not be governed by a rule it cannot read** | Limits discovered by hitting them; changes discovered by breakage |
| `R00.13.07` | Surfaces read **live state**, never a written description | The surface drifts from truth while looking authoritative |
| `R00.13.08` | A definition with no surface is a **finding**, enforced by a two-way coverage mirror | "No exceptions" becomes aspirational |
| `R00.13.09` | A surface showing something absent from the canon is an **undeclared rule** — the more dangerous direction | Rules accumulate in the interface that exist nowhere in governance |

### R00.13.1 — Standing

`R00.13.10` — **Fifteen of sixteen operator surfaces do not exist. Zero scope-facing surfaces exist.** The one that exists reads status files rather than live mechanisms, so it can be stale while appearing authoritative.

`R00.13.11` — By the prevention axiom (`U1.2.32.1`), a hidden definition is **silent by construction** — it produces no signal until it is violated. That places this in the absolute category, where prevention is chosen without cost comparison.

`R00.13.12` — **This is the human-side counterpart of the load doctrine.** `U1.2.31` addresses an actor holding too much. This addresses the same failure imposed on a person — who, unlike an actor, cannot be re-prompted, and whose chased definition eventually stops being chased.

---

## R00.14 — Journey definition and measurement awaiting ratification

### R00.14.1 — Journeys · `U6.2.10`

| # | Decision | If rejected |
|---|---|---|
| `R00.14.01` | **A named surface is not a design.** Journeys are defined before the surfaces serving them. | Twenty-one invented interaction models, inconsistent by construction |
| `R00.14.02` | Registers are data; **journeys are the interface**, ordered by question frequency | Navigable dead ends: each surface reachable, none a task |
| `R00.14.03` | Every flow carries seven fields before it is built: trigger · intent · entry · steps · exit · goal · measurable result | Actors invent the missing fields |
| `R00.14.04` | **Relationships are traversable** — a displayed address must be followable | The chase moves from chat into the interface |
| `R00.14.05` | Density designed at projected scale, not current scale | Complete lists that become exports |
| `R00.14.06` | Roles get separately defined journeys | One interface showing everything to everyone serves no one |

### R00.14.2 — Measurement · `U1.2.35`

| # | Decision | If rejected |
|---|---|---|
| `R00.14.07` | Six fields defined **before** work starts: metric · baseline · target · owner · cadence · evidence source | Measurement chosen after delivery reliably reports success |
| `R00.14.08` | **Baseline taken before the change** — it cannot be taken afterwards | Every later number is unanchored |
| `R00.14.09` | **Evidence source must exist**; creating it is part of the solution | Metrics that turn out to be unobtainable, discovered late |
| `R00.14.10` | **Two windows** — early at 2–4 weeks for adoption and friction, late at 3–6 months for value | A single review is always at the wrong time |
| `R00.14.11` | The late window is **scheduled at build time with a named owner**, or it does not happen | Value is never confirmed and nobody notices |
| `R00.14.12` | **A control that never fires is not measured but assumed** — firing rate is itself a metric | An always-passing check is trusted |
| `R00.14.13` | Surprises are the yield; **the null result is recorded, not omitted** | Measurement becomes confirmation |
| `R00.14.14` | Measurement is the **builder's** obligation, designed before building | Deferred permanently |

### R00.14.3 — Reviewer note

`R00.14.15` — **This corrects my own work, on the governor's challenge.** I produced twenty-one surface names and called it a specification. It defined nothing, and an actor building from it would have freestyled every interaction — which is what the governor predicted.

`R00.14.16` — **I violated `U6.2.07` L3 in the act of writing it**, ordering the surfaces by canon structure rather than by need. Recorded as a correction, not removed.

`R00.14.17` — **`I6.5.45` is time-critical.** Baselines cannot be taken retroactively. Any surface that ships before its baseline is measured against nothing, permanently.

---

## R00.15 — Canonical order, sustain, and actor disposition

### R00.15.1 — The canonical order · `P1.0.1`

| # | Decision | If rejected |
|---|---|---|
| `R00.15.01` | **Intent → Align → Goal → Build → Prove → Sustain**, stated as law at the head of the method | Order becomes convention and erodes under pressure |
| `R00.15.02` | **Align is its own stage**, with the actor's exclusions stated and explicit human confirmation | Misalignment is discovered after the work is built |
| `R00.15.03` | Measurement lives **inside** the goal, not after delivery | Baselines are lost permanently |
| `R00.15.04` | Twelve stages | — |

### R00.15.2 — Sustain · `P1.12`

| # | Decision | If rejected |
|---|---|---|
| `R00.15.05` | **Sustain is its own stage and runs forever** | A thing that worked once is assumed to work |
| `R00.15.06` | **Two triggers: calendar and change event** | Regression from additions elsewhere is found weeks later |
| `R00.15.07` | **Cadence assigned per check**, not from a blanket ladder | Noise trains the operator to ignore the channel |
| `R00.15.08` | **"Is this check still capable of failing?"** — every sustain run includes a deliberately failing input | A silently broken check reports clean forever |
| `R00.15.09` | A schedule ends by **decision**, never by quietly ceasing to run | Abandoned checks discovered only when what they guarded fails |

### R00.15.3 — Actor disposition · `U1.2.36`

| # | Decision | If rejected |
|---|---|---|
| `R00.15.10` | **Actor defaults are not relied upon.** The required disposition is defined; where it conflicts with a default, the definition wins. | The system inherits general-purpose agreeableness |
| `R00.15.11` | Ten required behaviours, D1–D10 | — |
| `R00.15.12` | **D3 — volunteer the objection nobody asked for** | The unconsidered objection stays unconsidered |
| `R00.15.13` | **D8 — hold register regardless of the human's affect** | Confidently presented ideas receive less scrutiny, which is inverted |
| `R00.15.14` | **Deference on decisions; none on facts or objections** | An actor that stops objecting when the human is confident has removed its only function |
| `R00.15.15` | Disposition is delivered **per task**, in the pocket | Stated at session start, absent when needed |

### R00.15.4 — Reviewer notes

`R00.15.16` — **I improved two things the governor specified**, and both should be reviewed as changes rather than accepted as instructions carried out:
1. *Verify 100% alignment* became **restate + state exclusions + explicit confirmation**. Mutual agreement without stated exclusions is theatre — both parties confirm and are wrong together.
2. *Daily/weekly/monthly/quarterly* became **assigned cadence plus a change trigger**. A uniform ladder produces noise on slow-changing things and negligence on fast-changing ones, and the governor's actual concern — damage from new additions — does not arrive on a calendar.

`R00.15.17` — **The pipeline is renamed *Intent to Sustained***, since it no longer ends at validation.

---

## R00.16 — Three-scope method and the alignment jewel

### R00.16.1 — The method · `U1.2.37`

| # | Decision | If rejected |
|---|---|---|
| `R00.16.01` | **Every change is worked at three scopes** — local, connected, root | Work stops at the element and the system stops learning from itself |
| `R00.16.02` | **S2 walks declared links**, asking break / strengthen / contradict for each | Ripple effects found later, by breakage |
| `R00.16.03` | **S3 has two halves — extract and propagate.** Extraction alone is decorative. | Wisdom accumulates and changes nothing |
| `R00.16.04` | Propagation scans **all** elements: where the truth already operates under another name, and where it must operate and does not | Independent restatements drift apart; absences stay invisible |
| `R00.16.05` | **Each embedding is assigned a tier; anything at T0/T1 is recorded as theatre** | A principle reads as adopted while being enforced nowhere |
| `R00.16.06` | **The theatre test:** name every place a principle is enforced, and the tier of each | "It is written in the guidance" passes as enforcement |

### R00.16.2 — The alignment jewel · `U1.2.38`

| # | Decision | If rejected |
|---|---|---|
| `R00.16.07` | Alignment is a **jewel governing every boundary**, not a pipeline stage | Eleven boundaries stay unverified |
| `R00.16.08` | Three parts everywhere: **understood · excluded · assumed** | Misalignment continues to hide in the exclusions |
| `R00.16.09` | Twelve boundaries named, B1–B12 | Coverage is decided case by case |
| `R00.16.10` | Four existing rules become **citations** of the one jewel, not restatements | Four independent versions drift |
| `R00.16.11` | **A restatement method that never surfaces a correction is not working** — it is producing agreement | Alignment becomes the ritual it exists to prevent |

### R00.16.3 — Eight embeddings required · `W01`

| # | Boundary | Tier |
|---|---|---|
| `R00.16.12` | Contract → builder: build cannot open until the contract is restated with exclusions | T3 |
| `R00.16.13` | Pocket → actor: acknowledgment section; constraints restated before producing | T3 |
| `R00.16.14` | Record → future reader: a **different** actor can act on it without asking the author | T3 |
| `R00.16.15` | Actor → actor: restate and confirm per hop | T2 |
| `R00.16.16` | Vendor → party: customisation boundary shown and acknowledged, not published | T3 |
| `R00.16.17` | Role → role: authority stated in the interface and acknowledged at assignment | T3 |
| `R00.16.18` | Observer → record: capture includes what the observer thought it meant | T2 |
| `R00.16.19` | Intent → validation: measured against the **confirmed restatement** | T3 |

### R00.16.4 — Standing

`R00.16.20` — **Two of twelve alignment boundaries are genuinely enforced.** One by a pipeline gate, one cryptographically. Two more are written and unenforced — theatre by the system's own test. Eight are absent.

`R00.16.21` — **This is the honest state of a principle that reads, across the documents, as fully adopted.** It is also the first demonstration that the theatre test works: nothing else in this session would have surfaced that number.

---

## R00.17 — Divergence framing and the charter

### R00.17.1 — Divergence · `U1.2.38.1b`

| # | Decision | If rejected |
|---|---|---|
| `R00.17.01` | **Divergence is the expected starting condition at every boundary, never a fault of either party** | Surfacing becomes costly; assumptions get hidden rather than tabled; the mechanism inverts into producing agreement |
| `R00.17.02` | **Fault attaches only to not surfacing**, never to having had a different prior | Actors under-report to avoid looking careless |
| `R00.17.03` | **Language is neutral**: divergence surfaced, priors exposed, reconciled — not misalignment, error, corrected | An actor primed to report faults reports fewer of them |
| `R00.17.04` | **Understanding is not binary.** Overlapping and non-overlapping regions; the work is mapping the boundary. | A pass/fail frame on something that has no pass state |
| `R00.17.05` | **An alignment stage repeating several times is working, not failing** | Repetition is read as friction and gets shortened |
| `R00.17.06` | **`W01.9`** — surfacing a divergence is recorded as a contribution, never as a correction against the surfacing party | The register itself becomes a disincentive |

### R00.17.2 — Charter · `01`

| # | Decision | If rejected |
|---|---|---|
| `R00.17.07` | The goal as stated — presence, tier, propagation, each falsifiable | The goal stays implicit and drifts |
| `R00.17.08` | **Phase 3 (apply to CISEM) does not begin before phase 2 (stabilise and run once) completes** | Two unknowns at once, with no way to tell which failed |
| `R00.17.09` | CISEM's live security repair is **not** phase 3 and is not gated | Security work waits on a documentation exercise |
| `R00.17.10` | Six goal metrics, `G1`–`G6`, with `G5` — one solution through the method — as the gate on all others | Every metric stays a projection |
| `R00.17.11` | **The charter states plainly that the system is currently at T1 by its own test** | The standing gets mistaken for readiness |

---

## R00.18 — Axiom sharpening, simulation, and the protocol framing

| # | Decision | If rejected |
|---|---|---|
| `R00.18.01` | **The axiom becomes "prevention and preservation, by improving creation"** | "Prevention" continues to be read as "more inspection" — the answer that feels like rigour and worsens the dominant failure class |
| `R00.18.02` | **Preservation is named as the second half** — holding what is correct, not only stopping what is new | Silent decay stays outside the axiom |
| `R00.18.03` | **"By improving creation" is the operative clause** — smaller steps, constraints present, lists enumerated, facts read, an honest exit. Inspection is the residue. | Detection inflation returns |
| `R00.18.04` | **Preservation runs on the loops** (`U1.2.34`), not as a state | It becomes an aspiration with no cadence |
| `R00.18.05` | **`P1.7` Simulate as its own stage**, with a ten-scenario minimum walk, **before** enforcement | Failure modes are discovered at verification, where they cost a rebuild |
| `R00.18.06` | **Simulation feeds enforcement** — what can go wrong must be known before what to prevent is chosen | Enforcement is designed against imagined failures |
| `R00.18.07` | **A walk that finds nothing was not a walk** — record which scenarios ran and what each produced | The stage becomes a checkbox |
| `R00.18.08` | **Simulate, verify, validate are three different questions at three moments** | They collapse into one, performed at the most expensive of the three |
| `R00.18.09` | **The subject is a protocol for building a platform**, not a system or framework | The distinction that makes it non-optional is lost — a framework is consulted, a protocol is the exchange itself |
| `R00.18.10` | **The protocol is the product; the canon is what it produces and reads back** | A protocol without a record repeats itself; a record without a protocol is an archive |
| `R00.18.11` | Thirteen stages | — |

---

## R00.19 — The leading axiom · `U1.2.32`

| # | Decision | If rejected |
|---|---|---|
| `R00.19.01` | **`U1.2.32` is the leading axiom.** Every other rule is a consequence of it or a mechanism serving it. | The canon has no ordering principle; rules compete on equal footing |
| `R00.19.02` | **Four parts, not two** — prevention · preservation · by improving creation · **toward one source of truth** | Accumulation stays outside the axiom, and it is the less-noticed decay path |
| `R00.19.03` | **Accumulation is a decay path.** A system fails through a second script and a third writer, not only through defects. | Individually defensible additions compound until nobody can hold the system |
| `R00.19.04` | **The four acts, mandatory order: learn → check what exists → consolidate → create** | Creation begins from the request rather than from the inventory |
| `R00.19.05` | **Act 2 is checked against source**, never memory, a document, or a summary | The inventory misses the item that mattered |
| `R00.19.06` | **Consolidation is the expected outcome; creation is the exception.** A high create-rate signals act 2 is not running. | Creation stays the default and the count of things grows with the count of requests |
| `R00.19.07` | **Five conditions for creation**, stated in writing before the thing exists | "A good idea" remains sufficient justification |
| `R00.19.08` | **Condition 3 — exactly one owner.** If something else already writes it, this is a duplication, not a creation. | The dominant duplication form stays invisible: not a second copy, a **second writer** |
| `R00.19.09` | **Condition 5 — a retirement condition.** Anything without one is permanent by default. | Nothing is ever removed and the system only grows |
| `R00.19.10` | **One source of truth is a direction, not a state.** Every act moves toward or away; there is no neutral addition. | "We will consolidate later" — which never arrives |
| `R00.19.11` | **A version number is not consolidation.** V1.1 shipped while V1.0 remains reachable creates two authorities. | Versioning is mistaken for superseding |
| `R00.19.12` | **A duplication costs more than a defect**, because it is never detected as one — it surfaces months later as confusion about which is real | Duplication continues to be treated as harmless |

### R00.19.1 — Evidence prompting this

`R00.19.13` — Observed in one working afternoon: two backfill versions · two linter versions · a script created in scratch, deleted, and recreated · two audit documents of one session · **a fourth uncoordinated writer of one table**. Every item individually defensible. Together, the consolidation cost now exceeds the work they performed.

`R00.19.14` — A schema constraint blocking the entire first-user path was discovered **while writing a new script**, not before it. Act 2 was not run.

---

## R00.20 — Canvass pass: insights extracted from applied work

**Method note:** this pass ran the four acts (`U1.2.32.3`). Act 2 inventoried each candidate against source before writing. **Result: four extensions to existing doctrines, one new doctrine, one grid addition, two pipeline scenarios.** The ratio is deliberate — consolidation is the expected outcome.

### R00.20.1 — Extensions to `U1.2.08` verification doctrine

| # | Addition | From |
|---|---|---|
| `R00.20.01` | **A pass on an empty set proves nothing.** "All members satisfy the condition" across zero members is vacuously true and indistinguishable from a real pass. | A dry-run reporting all clear against an empty table |
| `R00.20.02` | **Verify between the parts, not after them.** A multi-part operation reports complete while an early part failed; the intermediate state is never observed. | A file left syntactically invalid mid-edit, reaching a running process |
| `R00.20.03` | **A status is not evidence.** A column reading "verified" without naming what was run is an assertion in the shape of a result. | Five files marked "Modified & Verified"; four had no test |

### R00.20.2 — Extension to `U1.2.13`

| # | Addition |
|---|---|
| `R00.20.04` | **A silent success can be worse than a loud failure.** A visible failure is repaired within the hour; a silent substitution reads as working while work accumulates on top of it. **Between an outage and a silent degradation, the outage is the safer failure.** |

### R00.20.3 — Extension to `U1.2.31`

| # | Addition |
|---|---|
| `R00.20.05` | **The record outranks the exchange.** As an exchange lengthens its early parts degrade for every party. Once a durable record exists it is authoritative and the conversation is not. |

### R00.20.4 — Extension to `U1.2.32` — `U1.2.32.4b`

| # | Addition |
|---|---|
| `R00.20.06` | **Classify the danger; do not enumerate the exceptions.** Exclusion lists accumulate and become a record of past surprises. Classification improves once and holds for shapes not yet seen. **Test:** does the repair make the rule correct for a case nobody has met yet? |

### R00.20.5 — New doctrine · `U1.2.39` Path completeness

| # | Decision |
|---|---|
| `R00.20.07` | **A path is only as complete as its least-owned step.** Every other step being excellent changes nothing. |
| `R00.20.08` | **Every step in a path names its owner.** "It happens somewhere" is an unowned step — a gap in the definition, not in the code. |
| `R00.20.09` | **An element with no caller is not a capability.** Worse than absent: its presence makes the path look complete on an inventory. |
| `R00.20.10` | **A gate must define the state before the gate.** The recurring failure is circular — a gate requires what only the gate can issue. Both sides are correct and nobody can enter. |
| `R00.20.11` | **A fixture manufacturing an unreachable state is a mask, not a test.** It tests everything downstream of the gap and conceals the gap. Fixtures are created **through the real path wherever one exists**. |
| `R00.20.12` | **Paths are audited; components are checked.** A component review cannot find an unowned step, because the step belongs to no component. |

### R00.20.6 — Grid and pipeline

| # | Addition |
|---|---|
| `R00.20.13` | `U0B.2.02a` — **if a file must exist after the turn ends for work to continue, it is not scratch** |
| `R00.20.14` | `P1.7` scenarios 11 and 12 — **the set is empty** · **it is the first time.** The two most often absent: an empty set makes every check pass; a first instance is the only case with no prior state, and every party meets it exactly once. |

---

## R00.21 — Second axiom · `U1.2.40`

| # | Decision | If rejected |
|---|---|---|
| `R00.21.01` | **`U1.2.40` is the second axiom** — a lesson used once is a lesson wasted | Defects are corrected and their lessons discarded, and the same price is paid repeatedly |
| `R00.21.02` | **Three states: experienced · recorded · enforced.** Only the third closes the loop. | "We fixed it" passes as resolution |
| `R00.21.03` | **Every defect owes four artifacts** — correction · record · generalisation · enforcement | Stopping at correction stays the ordinary case |
| `R00.21.04` | **Where enforcement is absent, it is a recorded debt with an owner**, never a silent omission | Deferral without saying so |
| `R00.21.05` | **A surprise is worth more than a success** — including the positive and the null | Only failures are captured, and half the signal is lost |
| `R00.21.06` | **Capture at the moment** — the only loop with no downstream repair | Reconstruction keeps the outcome and loses the near-miss |
| `R00.21.07` | **The wasted-effort test** at the close of any work producing a defect or surprise: name the mechanism, or name the debt and its owner | "We will remember" — which guarantees recurrence |
| `R00.21.08` | **The axiom applies to process defects, not only product defects** — and process defects are the higher-leverage class | An actor skipping an inventory produces defects continuously and is never enforced against |
| `R00.21.09` | **`W02` established** — every defect, gap and surprise from applied work carried through the four artifacts | Lessons stay scattered across a conversation |

### R00.21.1 — Standing from `W02`

`R00.21.10` — **41 lessons captured · 17 enforced at T3–T5 · 3 at T2 · 14 recorded at T1 with enforcement outstanding.**

`R00.21.11` — **All fourteen open debts are enforceable by mechanisms that do not exist yet. None requires a decision — only building.**

`R00.21.12` — **`W02` is an instalment, not a closure.** By its own test, step 4 of `U1.2.40.2` is outstanding on fourteen of forty-one rows. Stating that is the document's purpose.

---

## R00.22 — Bounded execution and pre-action authorization · `U1.2.41`

**Grounded in researched industry practice**, not derived from this session alone. Convergence with independently-reached canon items is noted per decision.

| # | Decision | Converges with | If rejected |
|---|---|---|---|
| `R00.22.01` | **The loop is the problem, not the actor.** Actor defects are the predictable output of an ungoverned loop. | `U1.2.31` | Behaviour is addressed by asking for more care, which has failed every time |
| `R00.22.02` | **Three enforcement positions:** instruction · pre-action authorization · post-hoc check. Only the second prevents. | `U1.2.01` ladder | Post-hoc checking is mistaken for prevention |
| `R00.22.03` | **The threshold rule** — an irrecoverable consequence belongs in pre-action authorization, never in instruction | **`U1.2.32.8`, reached independently** | Credential disclosure stays governed by a written rule |
| `R00.22.04` | **The bypass test** — if the actor can be told to bypass a control, the control does not exist. No bypass mode. | `U1.2.01` T4 | Controls remain instructions wearing control names |
| `R00.22.05` | **Bounded work units** — declared entry · **fixed** capability set · declared exit · refusal outside the set | `U0C.2.02` pocket | The loop stays open and its defects recur |
| `R00.22.06` | **Interception first (immediate), boundedness second (structural).** Interception is a permanent tax that grows with the tool surface; boundedness does not. | `U1.2.32` part 4 | Either no floor, or a rule set that grows forever |
| `R00.22.07` | **A permission list is not a policy.** It records what was allowed before, not what should be allowed now. | `U1.2.32.4b` | The 923-entry list continues to be treated as a control |
| `R00.22.08` | **Authorization is placed outside the actor's reach.** A gate the actor loads or can rewrite is not a gate. | `U1.2.01` | Enforcement remains at the tier that fails |

### R00.22.1 — Standing

`R00.22.09` — **`I1.3.60`–`I1.3.64`: no pre-action authorization exists · the loop is unbounded · the allowlist is not a policy · irrecoverable actions are governed by instruction · host hook capability unverified and not assumed.**

`R00.22.10` — **Three canon items were independently confirmed by external practice**: the durability ladder's dismissal of prompt-level rules · the irrecoverable-consequence threshold · classify-over-enumerate applied to permissions. Convergence, not duplication (`U1.2.33.2`).

`R00.22.11` — **The governor's bucket/bubble framing is the bounded-execution pattern**, and it is the more structural of the two industry answers. It was arrived at before the research, from prior platform work.

---

## R00.23 — Self-description, compensation, and the product framing

### R00.23.1 — Self-description integrity · `U1.2.42`

| # | Decision | If rejected |
|---|---|---|
| `R00.23.01` | **A name, comment, or status must not assert a property the artifact lacks.** Three forms, one class. | Downstream parties inherit the error while believing they verified something |
| `R00.23.02` | **Absent beats false.** No comment is a known unknown; a false one reads as known and is relied on during an incident. | Stale claims survive because they look like documentation |
| `R00.23.03` | **A description is corrected in the same act as the behaviour**, or removed. If the behaviour cannot be fixed now, the description states the actual behaviour and names the debt. | The artifact keeps lying about itself while a fix is deferred |
| `R00.23.04` | **Comment-level claims are not mechanically checkable** and remain at instruction level, recorded as debt rather than assumed closed | Pretending a scanner catches it is the same defect one level up |

### R00.23.2 — Compensation · `U3.2.10a`

| # | Decision | If rejected |
|---|---|---|
| `R00.23.05` | **Compensation belongs only where rollback is structurally impossible.** Within one store it adds a second failure mode. | A partial write plus a failed cleanup, instead of a clean rollback |
| `R00.23.06` | **A partial write leaves records indistinguishable from legitimate ones**, and nothing will clean them up because nothing knows they are wrong | Orphan records accumulate silently and read as real |

### R00.23.3 — Bypass test extension · `U1.2.41.4`

| # | Decision |
|---|---|
| `R00.23.07` | **A control whose enforcement is a social contract is not a control.** Where the only thing preventing a violation is mutual agreement, the agreement is the mechanism — and agreements are T1. |
| `R00.23.08` | **A gate firing on artifact production rather than on the other party's acknowledgement confirms something was made, not that anyone agreed.** A gate whose asker and confirmer can be the same party is not a gate. |

### R00.23.4 — The product framing · `01` §1b

| # | Decision | If rejected |
|---|---|---|
| `R00.23.09` | **Two capabilities from one body of work: creating a platform, and auditing one.** Neither was the original intent; both are what the artifacts turn out to be. | The audit value stays latent and the registers read as retrospectives |
| `R00.23.10` | **The lesson registers are an audit instrument, not a history.** Every row is a runnable check. | 41 earned lessons remain a document nobody consults |
| `R00.23.11` | **What separates it from a generic checklist: every entry was earned by a real defect in a real system.** A generic checklist asks what someone imagined; this asks what actually happened. | The instrument's distinguishing property is not stated and gets diluted |
| `R00.23.12` | **Building correctly and auditing honestly ask the same questions at different times.** The audit instrument is the method's inventory and walk, applied to a system that already exists. | Two parallel bodies of work where there is one |
| `R00.23.13` | **The registers are worth only what their enforcement column says.** A problem list with no mechanism per row is a document describing vigilance. | The instrument becomes the theatre it was built to expose |

---

## R00.24 — Incidental correctness, one-way changes, escape values

**Method:** four acts run. Six candidates inventoried against source; **none had an existing home.** Three new doctrines, two extensions, one scenario.

### R00.24.1 — Incidental correctness · `U1.2.43`

| # | Decision | If rejected |
|---|---|---|
| `R00.24.01` | **Correct behaviour from an unrelated constraint is a coincidence, not a control** | Protection is assumed where none was designed |
| `R00.24.02` | **More dangerous than an absent control** — it presents as working and passes every outcome-based review | The gap is invisible until an unrelated change removes it |
| `R00.24.03` | **The test:** name the constraint producing the behaviour. Is that what it is for? | Two requirements silently share one mechanism |
| `R00.24.04` | **Repair by making it deliberate** — a named guard, so a later party finds it rather than silence | The next tidy-up removes it and nothing reports the loss |

### R00.24.2 — One-way changes · `U1.2.44`

| # | Decision | If rejected |
|---|---|---|
| `R00.24.05` | **Reversibility is a property of the data, not the schema.** Re-adding a column returns the structure, not the content. | Destructive changes are assessed as reversible |
| `R00.24.06` | **Two questions first, from source:** what exists there now · what reads it | The most expensive assumption is made silently |
| `R00.24.07` | **Prefer the reversible order** — relax first, remove later. They feel like one tidy-up; one is reversible and one is not. | Reversible and irreversible are bundled and applied together |
| `R00.24.08` | **Say the words:** *this is irreversible · what is lost is X · what was checked is Y* | Irreversibility is discovered after the fact |

### R00.24.3 — Escape values · `U1.2.45`

| # | Decision | If rejected |
|---|---|---|
| `R00.24.09` | **Every constrained vocabulary carries an explicit escape value**, as visible as the others | The first unanticipated case is forced into the nearest wrong category |
| `R00.24.10` | **Worse than free text** — a constrained value is trusted and aggregated, so every total including it is wrong and nothing indicates so | Silent corruption of every count |
| `R00.24.11` | **The escape value is itself a measurement.** Never used = complete or never applied. Common = outgrown. Both knowable only if it exists and is counted. | The vocabulary's health is unobservable |
| `R00.24.12` | **Classify by cause, not symptom.** Test: can a party read the value and know their next action? | Every consumer re-infers what to do from what was observed |

### R00.24.4 — Persistence extensions

| # | Decision |
|---|---|
| `R00.24.13` | `U3.2.06a` — **a schema built for one model does not serve another by adjustment.** Diagnostic: does it fail *sometimes* or *always*? An operation that can never succeed is not a defect in the operation. |
| `R00.24.14` | `U3.2.06b` — **retire columns asserting a responsibility the system has moved.** Not merely unused — an invitation for the next party to populate them, putting the responsibility in two places. |
| `R00.24.15` | `P1.7` scenario 13 — **it can never succeed.** Every path failing at the same point is an incompatibility, not a failure mode. |

### R00.24.5 — Evidence

`R00.24.16` — **A structural blocker was found by reading the schema, one turn after the four acts were made mandatory at stage 5.** Both prior turns would have written the migration and discovered it at execution. **The relevant fact is not that it was found — it is that it was found before the write rather than during it.**

---

## R00.25 — Prevention as the deliverable · `U1.2.46`

**Motivating evidence:** across a full working session, three debts were precisely specified, owned by the party who could build them, and **none was built.** Every unit closed with a fix and a record. `U1.2.40` required enforcement; nothing required it *by the close of the unit that incurred it.*

| # | Decision | If rejected |
|---|---|---|
| `R00.25.01` | **The unit of completion is the mechanism, not the fix.** A fix plus a record is progress, not completion. | The same defect class is carried for years while every instance is "closed" |
| `R00.25.02` | **Sweep the class in the same act.** A defect never arrives alone; the reasoning that produced it produced others. | Instances are found one at a time, at full price each |
| `R00.25.03` | **"Fixed the one that was reported" is an incomplete report, not a complete fix** | Partial sweeps read as closure |
| `R00.25.04` | **Enforcement is named before the fix is written.** Knowing the mechanism changes the fix — the shape admitting a mechanism is chosen over the shape that merely works. | Prevention is designed against a correction already made, or not at all |
| `R00.25.05` | **Debt is assigned to whoever can build it.** Recording against a party who cannot is a transfer of responsibility disguised as a record. | Debt survives by being someone else's |
| `R00.25.06` | **The register must shrink.** Growth across two consecutive units is a finding about the process, not the items. | An unmeasured backlog that only grows |
| `R00.25.07` | **The register count is stated at the close of every unit that touched it** | Its direction is never observable |
| `R00.25.08` | **Six things a unit may not close with** (`U1.2.46.6`) | Each is an accepted form of not finishing |
| `R00.25.09` | **This obligation is itself T1 and is recorded as debt** until a gate on the path enforces it | The rule about enforcing rules is assumed to enforce itself |

---

## R00.26 — Discovery and closure phases · `U1.2.47`

**Motivating evidence:** two consecutive units of complete, well-executed sweeping. Register 9 → 10 → 12. **Zero specified fixes applied.** Every finding real, every sweep finite, every item owned — and nothing closed.

| # | Decision | If rejected |
|---|---|---|
| `R00.26.01` | **Discovery and closure are declared phases**, stated before a unit begins | Parties measure different things without noticing |
| `R00.26.02` | **Register growth is correct in discovery and a failure in closure** — the same measurement, inverted | The signal is misread in both directions |
| `R00.26.03` | **Discovery terminates on a stated scope**, not on exhaustion | A sweep that cannot finish accumulates until attention runs out |
| `R00.26.04` | **Findings during closure are recorded and parked** — except one that makes the current closure wrong | A closure that pursues its findings never closes |
| `R00.26.05` | **Closure is a batch with one verification pass**, not a stream | Every boundary re-opens the discovery temptation |
| `R00.26.06` | **Discovery is more rewarding than closure** — new information versus work already understood. A party left to choose will discover indefinitely. | A growing, increasingly accurate register that changes nothing |
| `R00.26.07` | **A register that only grows is a record of work not done** | Diligence and completion become indistinguishable |

---

## R00.27 — Premise verification and change-record drift

**Motivating evidence.** A structural blocker — *"this insert will always fail on a NOT NULL constraint"* — was read from a change-record file, reasoned from across multiple units of work, used to select between two architectural paths, and **did not exist.** The column was already nullable. A second column named in the same file was absent entirely. A third table the file created had never been applied.

**Every unit that reasoned from it reasoned correctly.**

### R00.27.1 — Premise verification · `U1.2.48`

| # | Decision | If rejected |
|---|---|---|
| `R00.27.01` | **A premise is tested at its point of entry, never at its point of consequence** | The cost becomes the volume of work built on it |
| `R00.27.02` | **A chain of correct inferences from a false premise looks exactly like good work** — no step-level check can detect it | Quality of reasoning is mistaken for validity of conclusion |
| `R00.27.03` | **Blast distance:** the cost is the work built before detection, not a fixed amount | Entry-point verification reads as optional thoroughness |
| `R00.27.04` | **Premises about live state are verified against live state**, never against an artifact describing it | A once-accurate artifact is trusted in proportion to how accurate it used to be |
| `R00.27.05` | **A dependent unit's first act is testing its premise**, not reasoning from it | The premise is tested by consequence |
| `R00.27.06` | **Premises carry source and date.** A fact cannot be audited; a citation can. | The next reader inherits the risk invisibly |
| `R00.27.07` | **Written is not applied** — the least suspected stale premise, because the record is authored by the party who intended to apply it | Intent and state stay indistinguishable |

### R00.27.2 — Persistence · `U3.2.09a`, `U3.2.09b`

| # | Decision |
|---|---|
| `R00.27.08` | **A change record is not a schema.** It records intent, in intended order — not application, supersession, or omission. |
| `R00.27.09` | **Four drift forms, none of which announces itself:** absent column · absent table · different constraint · out-of-band addition |
| `R00.27.10` | **Structure is drift-checked, not only access rules.** Where a check exists for one aspect, its absence for the others is a gap, not a scope decision. |
| `R00.27.11` | **Structural drift is the more consequential**, because every access rule is written against a structure |
| `R00.27.12` | **A migration authored while drift is unresolved is authored against an unknown** |

### R00.27.3 — Standing

`R00.27.13` — **Four migrations were drafted today from a change record treated as a schema. Every one is wrong in some respect. None was applied** — which is the only reason the cost was a query rather than an outage.

`R00.27.14` — **The structural drift check should have existed before the first migration was drafted.** It is now a prerequisite, not a deferred mechanism.

---

## R00.28 — Reconciliation of unavoidable duplication · `U1.2.49`

**Motivating evidence.** Every significant finding in one working session was the same class: **two representations of one truth, with nothing reconciling them.**

| Pair | Discovered by |
|---|---|
| Change record vs live schema | a query, after four migrations were drafted from the record |
| Claimed applied vs actually applied | the same query — two migrations reported landed had never run |
| Four role surfaces | a source sweep, after provisioning was specified to write one of them |
| A derived flag vs the condition it summarises | a code sweep |
| A comment vs the code beneath it | review |
| A variable name vs the privilege it held | review |

**Not one was found by a check. Every one was found by a person looking.**

| # | Decision | If rejected |
|---|---|---|
| `R00.28.01` | **Where duplication cannot be removed, a recurring reconciliation check is mandatory** | The one-authority rule reads as satisfied while divergence runs unobserved |
| `R00.28.02` | **Five classes of unavoidable duplication named** — description of a live system · claim and evidence · cache and source · derived flag and condition · declaration and subject | Each is rediscovered as a novel problem |
| `R00.28.03` | **Two obligations, neither substituting for the other:** declare the authority · build the check | Declaring feels like resolving, and the check is never built |
| `R00.28.04` | **The check is built at the moment the second representation is created** — the only moment both are known to agree | A divergence with an unknown start date |
| `R00.28.05` | **Two triggers: change and calendar.** Calendar misses nothing that code observed; change misses everything out of band. | The out-of-band change — the one nothing watched — goes unseen |
| `R00.28.06` | **Disagreement is reported, never auto-resolved** | The evidence of how it arose is destroyed, and an unratified decision is made silently |
| `R00.28.07` | **A reconciliation that has never disagreed is unverified** | It is indistinguishable from comparing a thing to itself |

---

## R00.29 — Mechanism liveness · `U7.2.11`

**Motivating evidence.** A continuous enforcement daemon orchestrating four components stopped via keyboard interrupt — a closed terminal. **Nothing reported it for four days.** Its state file continued to read `"status": "running"` with a process id that no longer existed.

**And within it:** one of the four components had been exiting non-zero on **every** run, with an **empty error message**, for the entire visible log history. The orchestrator logged the failure and continued. Every loop reported success.

| # | Decision | If rejected |
|---|---|---|
| `R00.29.01` | **Liveness is separate from correctness.** A proven mechanism that is not running enforces nothing. | Correctness is mistaken for operation |
| `R00.29.02` | **The common cause is interruption, not failure** — a closed window, an ended session | Supervision is designed against crashes and misses the ordinary case |
| `R00.29.03` | **Three obligations: heartbeat · watcher · restart.** A heartbeat with no watcher is a record nobody reads. | The state file says *running* indefinitely |
| `R00.29.04` | **A process cannot report its own death.** Liveness is established from outside. | The last thing written is believed forever |
| `R00.29.05` | **A component's non-zero exit fails the loop**, or is explicitly permitted with a recorded reason | Partial execution is reported as success |
| `R00.29.06` | **An empty error message is worse than none** — it reads as a handled case | The failure is invisible even to someone reading the log |
| `R00.29.07` | **Repeated identical failures escalate.** The same component failing every run is dead, not flaky. | A dead component runs indefinitely inside a loop reporting success |
| `R00.29.08` | **Lifetime is bound to the system, not a session.** Where impossible, the liveness check is mandatory. | Enforcement inherits the accidents of a terminal window |
