# Website Sandbox Category

1.1. **Purpose**:
This placeholder holds sandbox projects relating to CMS integrations, main portal layouts, navigation models, and custom client-side web components.

1.2. **Promotion Rule**:
Before promoting to `src/app/` or `src/components/`, the module must resolve all TypeScript strict types and satisfy Section 5 of the Enterprise Blueprint.
